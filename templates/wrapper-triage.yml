# AI Triage Wrapper Workflow
# Place this file at .github/workflows/ai-triage.yml in each target repository.
#
# Triggers when:
#   - An issue receives the 'ai:triage' label (including via the issue form)
#   - A comment contains '/ai triage'
#
# Required secrets in the target repository:
#   GH_PAT            — GitHub Personal Access Token (for private workflow-kit repo)
#   ANTHROPIC_API_KEY — Anthropic API key (if using claude provider)
#   OPENAI_API_KEY    — OpenAI API key (if using openai/codex/aider provider)

name: AI Triage

on:
  issues:
    types: [labeled]
  issue_comment:
    types: [created]

jobs:
  # Triggered by label
  triage-on-label:
    name: Triage on label
    if: >
      github.event_name == 'issues' &&
      github.event.label.name == 'ai:triage'
    uses: YOUR_ORG/github-ai-workflow-kit/.github/workflows/reusable-ai-triage.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repo: ${{ github.repository }}
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

  # Triggered by /ai triage comment
  triage-on-comment:
    name: Triage on comment
    if: >
      github.event_name == 'issue_comment' &&
      contains(github.event.comment.body, '/ai triage') &&
      github.event.issue.pull_request == null
    uses: YOUR_ORG/github-ai-workflow-kit/.github/workflows/reusable-ai-triage.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repo: ${{ github.repository }}
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
