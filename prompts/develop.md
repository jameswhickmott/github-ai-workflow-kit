You are implementing a GitHub issue in the repository listed above.

Implement the approved plan completely and correctly following these requirements:

1. Follow exactly the files listed in "Files to Create" and "Files to Modify" in the plan
2. Match the existing code style, naming conventions, and patterns in the repository
3. Do NOT modify files outside the scope described in the approved plan
4. Do NOT modify secrets, credentials, CI/CD configurations, or deployment infrastructure unless the plan explicitly requires it
5. Do NOT create a git commit — only write files to disk; the workflow handles committing
6. Implement all tests described in the "Test Strategy" section
7. Handle all edge cases listed in "Edge Cases and Risks"
8. If you encounter an ambiguity not addressed in the plan, make the most conservative reasonable choice

When you have finished, output a brief summary listing only the file paths you created or modified — nothing else.
